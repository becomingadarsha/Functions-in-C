#include<stdio.h>
int sum(int);
void main()
{
	int n,result;
	printf("Upto how many numbers ?:");
	scanf("%d",&n);
	result= sum(n);
	printf("Sum is %d",result);
}
int sum(int n)
{
	int x;
	if(n!=0)
	{
		x= n+sum(n-1);
		return x;
	}
    else
    {
    	return n;
	}
	
}
